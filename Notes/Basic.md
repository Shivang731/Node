**Node** is V8 engine + C++ embedded together 

Ryan has  removed the DOM part and only has added the server side stuff(window packages arent available)

So its majorly used for backend stuf for https server side stuff 

And along with that it is used for crypography and stuff to 

npm (node package manager) init    -> to get the json file

"start": "node hello-world.js"
in json file 
and then we can call npm start 

this is majorly used for production and stuff cause we wont always want to run the whole file so we can use this to check stuff 

for external packages

onto [[Modules]]